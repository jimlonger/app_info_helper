name: app_info_utils_example
description: Demonstrates how to use the app_info_utils plugin.
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  app_info_utils:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
